# 로컬 스크래핑 실행 시 requirements.txt와 함께 설치:
#   pip install -r requirements.txt -r requirements-scraper.txt
#
# Streamlit Cloud에는 requirements.txt + packages.txt만 사용됩니다.
# (selenium/curl_cffi는 Cloud에서 불필요)

# ── HTTP / 스크래핑 ──────────────────────────────────────────────
curl_cffi
fake-useragent

# ── Selenium ─────────────────────────────────────────────────────
selenium
seleniumbase
webdriver-manager
